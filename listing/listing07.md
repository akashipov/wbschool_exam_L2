Что выведет программа? Объяснить вывод программы.

```go
package main

import (
	"fmt"
	"math/rand"
	"time"
)

func asChan(vs ...int) <-chan int {
	c := make(chan int)

	go func() {
		for _, v := range vs {
			c <- v
			time.Sleep(time.Duration(rand.Intn(1000)) * time.Millisecond)
		}

		close(c)
	}()
	return c
}

func merge(a, b <-chan int) <-chan int {
	c := make(chan int)
	go func() {
		for {
			select {
			case v := <-a:
				c <- v
			case v := <-b:
				c <- v
			}
		}
	}()
	return c
}

func main() {

	a := asChan(1, 3, 5, 7)
	b := asChan(2, 4 ,6, 8)
	c := merge(a, b )
	for v := range c {
		fmt.Println(v)
	}
}
```

Ответ:
```
Частично все отработает так как планировалось, выведутся эти элементы сначала в каком то порядке неопределенном(так как селект не гарантирует порядка)
Затем мы уже будем получать бесконечно 0 и зациклимся. Так как закрытие каналов говорит нам о том что будут на ридер приходить(точнее будут считываться) нули постоянно при запросе, поэтому важно чекать второе возвращаемое значение

Мы можем избежать тут проблем, работая с ok параметром. либо ничего не делать, либо собирать флаги о каждом канале и если все флаги говорят нам о том что везде закрыто то надо выйти из цикла ридера
```
